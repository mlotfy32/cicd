import 'package:flutter/material.dart';

class Flavours extends StatelessWidget {
  const Flavours({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
